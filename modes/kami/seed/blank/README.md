# Blank

An empty paper with kami design tokens preloaded. Start typing inside
`<div class="page">…</div>`.

Paper size is locked to **{{paperSize}} {{orientation}}** — set when this
workspace was created.
