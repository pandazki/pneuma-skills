# Next plans for ClipCraft × Pneuma-Craft

## Completed

- **Plan 1 — Bootstrap** (`2026-04-12-clipcraft-craft-bootstrap.md`) — rename legacy, scaffold new `clipcraft` mode on `@pneuma-craft/*`, verify end-to-end launch with a stub viewer, baseline contract test.
- **Plan 2 — Domain + store** (`2026-04-12-clipcraft-plan2-domain-store.md`) — `AssetStatus` first-class in craft-core (async AIGC lifecycle), `project.json` schema with validator, hydration-via-events loader, `StateDump` text renderer, E2E agent-edit simulation.
- **Plan 3a — ID stability** (`2026-04-12-clipcraft-plan3a-id-stability.md`) — `asset:register` / `composition:add-track` / `composition:add-clip` accept optional explicit id with duplicate rejection; persistence layer passes on-disk ids through; full-stack hydration integration test.
- **Plan 3b — Write path + loop protection** (`2026-04-13-clipcraft-plan3b-write-path.md`) — `serializeProject` + `formatProjectJson`, round-trip fidelity tests, `writeProjectFile` client using existing `POST /api/files`, `useProjectSync` bidirectional hook with debounced autosave, parent-owned `lastAppliedRef` + state-based `providerKey` for loop protection.
- **Plan 3c — dispatchEnvelope + createdAt round-trip** (`2026-04-13-clipcraft-plan3c-dispatch-envelope.md`) — expose `dispatchEnvelope(envelope)` on `CraftCore`, `TimelineCore`, and the Zustand `PneumaCraftStore`; wire clipcraft's persistence loader + the bidirectional sync hook to use it so `asset.createdAt` survives hydration. Also fixed a hidden `title` round-trip bug exposed once `createdAt` no longer dominated drift. **Zero-POST invariant verified E2E** on fresh mount (was 1 in Plan 3b) — 592/592 tests pass.
- **Plan 4 — Playback + preview** (`2026-04-13-clipcraft-plan4-playback.md`) — mount a `<canvas>` via `@pneuma-craft/react`'s `PreviewRoot` render-prop and drive play/pause/seek through `usePlayback()`; build a workspace `AssetResolver` that maps opaque craft asset ids to `/content/<uri>` URLs via a mutable id→uri map; seed a real playable clip in `project.json` with a bundled 1×1 sample JPEG (later swapped for a 5s testsrc2 MP4 in Plan 5 pre-task). The mode does NOT call `engine.load()`, `engine.subscribeToFrames()`, or own the engine lifecycle — all of that is upstream's responsibility once the composition is in the store. **Smoke-tested under React StrictMode + Vite dev**: canvas mounts, hydration runs, first Play click advances time 0 → 5s, no `Store destroyed` errors. Exposed and fixed one upstream bug: `PneumaCraftProvider`'s store cleanup was not StrictMode-safe — patched on `feat/clipcraft-aigc-status` with a setTimeout-deferred destroy. Also exposed a rAF drift-skip deadlock in `PlaybackEngine` on real H.264 MP4s — fixed upstream. 653/653 tests pass.
- **Plan 6 — Legacy viewer replication** (`2026-04-14-clipcraft-plan6-legacy-replication.md`) — faithful replication of the entire legacy ClipCraft viewer shell onto craft's domain model. Superseded the earlier "Plan 6 = TimelineOverview3D" / "Plan 7 = DiveCanvas" split in the upcoming list because the legacy UI is one cohesive shell and splitting it made no sense after gap analysis. Seven tasks landed in one overnight subagent-driven run: (1) Scene logical layer + persistence — `ProjectScene` type, `SceneContext`/`useScenes`, pure `resolveScene(scene, allClips)` helper so downstream code can iterate without calling hooks in loops; (2) `ClipCraftLayout` + `TimelineShell` shell with collapse/overview/exploded/dive modes and a floating ↑/↓ toggle; (3) `VideoPreview` + `CaptionOverlay` mounted on `<PreviewRoot>` with render-prop canvas ref, `useActiveSubtitle` walking subtitle tracks at `currentTime`; (4) `AssetPanel` Model B — craft asset registry IS the full material library (4 tabs: Images / Clips / Audio / Text), no filesystem scan, `useAssetActions` uploads via `POST /api/files?origin=external` + `asset:register` + `provenance:set-root`; (5) `TimelineOverview3D` — framer-motion 12 CSS 2.5D (perspective / rotateX/rotateY) with Front/Side presets, `VideoLayerContent` / `CaptionLayerContent` / `AudioLayerContent`, `FakeWaveform`; (6) `ExplodedView` — per-layer scaled stack, scroll-wheel to shift focus, `useCurrentFrame` subscribes to `subscribeToFrames` and redraws a hidden canvas; (7) `DiveCanvas` — `@xyflow/react@12` + `@dagrejs/dagre@1` DAG over `coreState.provenance.edges`, `VariantPointerContext` so `NodeShell`'s "Use This" updates the active variant for a clip. Fixed multiple plan-code bugs inline: hook-in-loop from `useSceneResolver` inside `useMemo` (replaced with pure `resolveScene`), `TreeNodeData` missing xyflow's `Record<string,unknown>` index signature, `AssetMetadata` strict field set (`originalName` moved to `operation.params`), `RenderedFrame.image` (not `.bitmap`), `useWorkspaceAssetUrl` signature widened to accept `null`. Pre-existing `ClipCraftPreview` provider nesting extended to `SyncedBody → TimelineZoomProvider → TimelineModeProvider → SceneProvider → VariantPointerProvider → AssetErrorsProvider → PneumaCraftProvider`. 698/698 tests pass at the end of Plan 6. **Browser-verified via `chrome-devtools-mcp`**: all four timeline modes (collapsed/overview/exploded/dive), VideoPreview playback 0→5s with painted canvas, AssetPanel 4 groups, Overview3D Front/Side presets + layer toggles (Video/Caption/Audio), ExplodedView side camera, DiveCanvas back/prev/next navigation, Script tab showing 2 seed scenes. Known deferred polish (per user "后面再一起修"): expanded mode hides VideoPreview play controls (want floating control bar), ExplodedView "Capturing frame" relies on active playback, video dive xyflow tree not exercised in test because time fell past clip range.
- **Plan 5.5 — Clip editing on timeline** (`2026-04-14-clipcraft-plan5.5-clip-editing.md`) — make the Plan 5 read-only timeline interactive. The ripple+snap drag algorithm was copied verbatim from `@pneuma-craft/react-ui/src/timeline/timeline-track.tsx` into a mode-local `timeline/dragEngine.ts` with unit tests; react-ui stays unconsumed. Reusable hooks: `useTrackDragEngine` (document-level mousemove/mouseup state machine with snap + ripple preview, dispatches a single `composition:move-clip` on release), `useClipResize` (left/right edge drag anchoring the opposite edge; dispatches `composition:trim-clip` always and `composition:move-clip` if the start shifted), `useTimelineShortcuts` (window keydown, bails out inside editable targets, wires Delete / S / ⌘Z / ⌘⇧Z). All three track components (`VideoTrack`, `AudioTrack`, `SubtitleTrack`) gained drag + 6px edge resize handles with a `grabbing` cursor toggle and an orange snap guide line. New `toolbar/EditToolbar.tsx` next to zoom controls renders Split / Delete / Collapse Gaps / Undo / Redo, gated on selection + `useUndo()`. `toolbar/collapseGaps.ts` walks each track in startTime order and emits sequential `move-clip`s to pack clips against 0. **Browser-verified via `chrome-devtools-mcp`** on a fresh `/tmp/clipcraft-plan5.5` workspace: (1) drag-to-move dispatched `composition:move-clip` and persisted to `project.json`, (2) click Split at playhead=2.4s produced two clips with `clip-1` inPoint 0→out 2.406 and a new clip inPoint 2.406→out 5 (confirming craft's splitter correctly carries inPoint/outPoint across the boundary), (3) Delete removed the selected half, (4) Collapse Gaps moved the orphan from start=2.406 back to start=0 with a single `move-clip`, (5) right-edge resize shrank duration 2.593→2.018s with `outPoint` → 4.424 and `inPoint` untouched, (6) left-edge resize shifted start 0→0.501 while `inPoint` 2.407→2.907 and right anchor (startTime+duration) held constant. 717/717 bun tests pass, `bun run tsc --noEmit` clean for ClipCraft files. **Known dev-only quirk**: under React StrictMode in Vite dev, a single drag occasionally dispatches two `move-clip` events (observed 0→2.0s instead of 0→1.0s); this is `useEffect` double-mount behavior and does not reproduce under production build. **Known pre-existing craft gotcha** (not plan 5.5): the undo manager wraps hydration events, so clicking Undo enough times undoes the initial composition-created / track-added / clip-added events and empties the project — Plan 5.5 did not fix this because it's a craft-store-level design issue for the upstream team. Multi-select is intentionally deferred. Toast-on-edit was dropped from scope (event log + composition state already reflect every edit).
- **Plan 5 — Timeline UI (read-only, legacy-faithful port)** (`2026-04-13-clipcraft-plan5-timeline.md`) — port the visual timeline from `clipcraft-legacy/viewer/timeline/` onto the craft store. The original Plan 5 implementation wrote 4 of the components from scratch with inline styles that didn't match legacy's compact layout; after visual inspection it was **reworked** to be a faithful port. New `modes/clipcraft/viewer/timeline/` contains: `Timeline.tsx` (legacy shell — compact 10px-font zoom header, "scroll / ⌘+scroll to zoom" hint, 2px GAP between rows, 32px `LABEL_W` with per-type emoji 🎬 / 🔊 / Tt), `VideoTrack.tsx` (absolute-positioned clips with 4px padding, filmstrip thumbnails via `useFrameExtractor`, subtle `rgba(249,115,22,0.3)` selection border — **no** chunky orange fill), `AudioTrack.tsx` (waveform bars), `SubtitleTrack.tsx` (ported from legacy `CaptionTrack`), `WaveformBars.tsx`, `TimeRuler.tsx` (ticks clamped to `[0, duration]`), `Playhead.tsx`, `TrackLabel.tsx` (verbatim 32px label), `hooks/useTimelineZoom.ts` (functional-updater setters so rapid `zoomIn` clicks accumulate correctly), `hooks/useFrameExtractor.ts`, `hooks/useWaveform.ts`. Click on ruler → `usePlayback().seek`; click on clip → `useDispatch(("human", { type: "selection:set", selection: { type: "clip", ids: [clipId] }}))`. Zoom is UI-only local state, not persisted. Ctrl/meta+wheel zoom uses a native passive-false listener so browser page zoom stays blocked. **Pre-task fix**: EditorPanel saves now pass `?origin=external` so writes through Pneuma's built-in editor reach the viewer's `Source<T>` as external events instead of being dropped as self-echoes. **Smoke-tested via `chrome-devtools-mcp`** after rework: filmstrip renders Cliper.mp4 thumbnails across 15s, click-to-seek lands within a pixel of the click position (60% → 8.65s on a 15s timeline), click-on-clip applies the subtle legacy selection border, 3× rapid zoom-in correctly accumulates (124 → 272 px/s = 124 × 1.3³), ruler no longer emits negative tick labels.

## Superseded

- **Plan 3d — Bidirectional sync transport (originally scoped, now absorbed upstream)** — Plan 3d would have hardened the mode-local `useProjectSync` hook with a proper transport contract. Instead, the upstream `feat/source-abstraction` work landed on `main` and introduced a first-class `Source<T>` abstraction with a `json-file` provider and server-side `pendingSelfWrites` origin tagging. ClipCraft was migrated to consume `sources.project` via `useSource` (commits `5319436`, `7582009`, `42bca41`, `06204f6`), deleting the entire mode-local sync layer (the bidirectional sync hook, the `externalEdit` helper, the `api-client`, and their tests). The "three-ref dance" is gone, and Plan 3b's load-bearing coupling between parent ref reset and provider remount no longer exists. See `docs/superpowers/plans/clipcraft-source-migration.md` and `docs/migration/2.29-source-abstraction.md`.

## Known limitations (to address in later plans)

- **"Full re-dispatch on external edit"** still kills in-memory state (undo history, PlaybackEngine playhead position) on every agent-originated file change. The source migration tags origin precisely (so the viewer knows *that* a change is external), but the response is still "bump providerKey, remount the craft store, re-hydrate from scratch". This is a craft-store-level concern — `Source<T>` cannot solve it. Plan 4 made this pain real (a live playing clip drops back to 0s and re-loads on every agent edit); a follow-up plan will replace the remount strategy with diff-and-dispatch.
- **Plan 5 leaves clip-level editing unwired.** The timeline is read-only for composition shape — click-to-select works, but drag-to-move, resize, and split do not. Plan 5.5 will port the ripple+snap drag engine from `@pneuma-craft/react-ui/src/timeline/timeline-track.tsx` by **copying the algorithm** into ClipCraft. react-ui stays unconsumed as a direct dep.
- **Still no automated test coverage for the rendering / timeline path itself.** The preview + timeline components are thin wrappers over craft hooks and pure data hooks from legacy. Verification is tsc + import-smoke tests + browser smoke tests via `chrome-devtools-mcp`. Once clip-edit lands and the drag math becomes non-trivial, install happy-dom + testing-library and cover the ripple/snap algorithm.

## Upcoming

To be written one at a time. Each plan should produce working software on its own and land on top of the previous one.
- **Plan 8 — Export** — replace `server/ffmpeg.ts` with `@pneuma-craft/video`'s `ExportEngine` in the browser; decide fallback strategy for long videos or large assets.
- **Plan 9 — MCP tool integration** — wire the existing MCP scripts (imagegen / videogen / tts / bgm) into craft's `generate` / `derive` provenance operations. `storyboard.json` / `graph.json` stop being first-class and become projections of craft state (if kept at all).
- **Plan 10 — Skill rewrite** — rewrite `modes/clipcraft/skill/SKILL.md` against the real craft domain model and agent workflow (action vocabulary, locator format, constraint set).

## Rules of engagement

Before each plan:

1. Survey what `clipcraft-legacy` does for the relevant surface.
2. Decide if a new concept belongs in the mode or upstream in `@pneuma-craft/*`. Discuss with the user.
3. Write the plan, verifying all referenced types against real `.d.ts` files (not the survey).
4. Use subagent-driven execution with combined spec + quality reviews between tasks.

`@pneuma-craft/*` lives at `/Users/pandazki/Codes/pneuma-craft-headless-stable` and is under active development (`0.1.0`, "not yet stable"). Patches to the packages land there on branch `feat/clipcraft-aigc-status`, not in this repo.
