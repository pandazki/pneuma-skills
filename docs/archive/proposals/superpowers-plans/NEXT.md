# Next plans for ClipCraft × Pneuma-Craft

## Completed

- **Plan 1 — Bootstrap** (`2026-04-12-clipcraft-craft-bootstrap.md`) — rename legacy, scaffold new `clipcraft` mode on `@pneuma-craft/*`, verify end-to-end launch with a stub viewer, baseline contract test.
- **Plan 2 — Domain + store** (`2026-04-12-clipcraft-plan2-domain-store.md`) — `AssetStatus` first-class in craft-core (async AIGC lifecycle), `project.json` schema with validator, hydration-via-events loader, `StateDump` text renderer, E2E agent-edit simulation.
- **Plan 3a — ID stability** (`2026-04-12-clipcraft-plan3a-id-stability.md`) — `asset:register` / `composition:add-track` / `composition:add-clip` accept optional explicit id with duplicate rejection; persistence layer passes on-disk ids through; full-stack hydration integration test.
- **Plan 3b — Write path + loop protection** (`2026-04-13-clipcraft-plan3b-write-path.md`) — `serializeProject` + `formatProjectJson`, round-trip fidelity tests, `writeProjectFile` client using existing `POST /api/files`, `useProjectSync` bidirectional hook with debounced autosave, parent-owned `lastAppliedRef` + state-based `providerKey` for loop protection.
- **Plan 3c — dispatchEnvelope + createdAt round-trip** (`2026-04-13-clipcraft-plan3c-dispatch-envelope.md`) — expose `dispatchEnvelope(envelope)` on `CraftCore`, `TimelineCore`, and the Zustand `PneumaCraftStore`; wire clipcraft's persistence loader + the bidirectional sync hook to use it so `asset.createdAt` survives hydration. Also fixed a hidden `title` round-trip bug exposed once `createdAt` no longer dominated drift. **Zero-POST invariant verified E2E** on fresh mount (was 1 in Plan 3b) — 592/592 tests pass.
- **Plan 4 — Playback + preview** (`2026-04-13-clipcraft-plan4-playback.md`) — mount a `<canvas>` via `@pneuma-craft/react`'s `PreviewRoot` render-prop and drive play/pause/seek through `usePlayback()`; build a workspace `AssetResolver` that maps opaque craft asset ids to `/content/<uri>` URLs via a mutable id→uri map; seed a real playable clip in `project.json` with a bundled 1×1 sample JPEG (later swapped for a 5s testsrc2 MP4 in Plan 5 pre-task). The mode does NOT call `engine.load()`, `engine.subscribeToFrames()`, or own the engine lifecycle — all of that is upstream's responsibility once the composition is in the store. **Smoke-tested under React StrictMode + Vite dev**: canvas mounts, hydration runs, first Play click advances time 0 → 5s, no `Store destroyed` errors. Exposed and fixed one upstream bug: `PneumaCraftProvider`'s store cleanup was not StrictMode-safe — patched on `feat/clipcraft-aigc-status` with a setTimeout-deferred destroy. Also exposed a rAF drift-skip deadlock in `PlaybackEngine` on real H.264 MP4s — fixed upstream. 653/653 tests pass.
- **Timeline Redesign — creation-first polish** (`2026-04-14-clipcraft-timeline-redesign.md`) — pulled playback transport out of VideoPreview into a global `TransportBar` (play / pause / goto start+end / speed dropdown, Space / Home / End shortcuts) mounted at the top of `Timeline.tsx` so it stays visible when Overview/Exploded/Dive is expanded. Regrouped the edit toolbar into Edit (Split / Delete / **Duplicate** / **Ripple Del** / Collapse) → `|` → History (Undo / Redo); added `D` keyboard shortcut for Duplicate and `⌘⌫` for Ripple Delete; new pure `buildRippleDeleteCommands(composition, clipId)` helper that emits `remove-clip` + `move-clip`s to shift later clips in the same track left by the removed duration (4 unit tests). `TrackLabel` rewritten from a 32px emoji-only spacer to a 140px column rendering `[icon] name [mute] [lock] [visibility]` with inline toggle buttons dispatching `composition:toggle-track-{mute,lock,visibility}`. New `useClipProvenance(clip)` hook walks `coreState.provenance.edges`, picks the most recent incoming edge by timestamp, and returns a `{ operation, summary }` pair; pure `formatOperation(assetName, op)` formatter (5 unit tests) renders a two-line `"Asset Name\ngenerate · sdxl · \"prompt…\""` string used both as the clip wrapper's `title` tooltip (VideoTrack / AudioTrack / SubtitleTrack) and as the inspector's source summary. **`ClipInspector`** — bottom-anchored popover strip mounted inside `TimelineShell` between the 3D panel and Timeline, renders three groups separated by 1px dividers: `[In] [Out] [Dur]` numeric editors (debounced `composition:trim-clip` dispatch on blur with `duration ≥ 0.1` clamp) | `VariantSwitcher` chips (reads `useVariants(assetId)`, dispatches `composition:rebind-clip` on pick, auto-hides when no sibling assets exist) | source summary text. Inspector returns `null` entirely when `selection.type !== "clip"` so no layout space is reserved. SubtitleTrack required extracting an inner `SubtitleClip` sub-component because hooks cannot be called inside a `map()` callback. **Browser-verified via `chrome-devtools-mcp`** on fresh `/tmp/clipcraft-redesign`: TransportBar renders with ⏮ ⏯ ⏭ + `00:00.00 / 00:05.00` + Speed dropdown, Space toggles play (0 → 1.19s in 1.2s → pause), End seeks to 5.0, Home seeks to 0, transport stays visible under ↑ expand, Duplicate button and D key both increment clip count (1 → 2 → 3), Ripple Del button removes leftmost clip and shifts remainder left, mute button dispatches `track-mute-toggled` and `track.muted` becomes true in project.json, clip `title` attribute shows `"Sample Clip\\nimport · bundled seed image"`, ClipInspector mounts on selection with CLIP · id label + In/Out/Dur inputs + SOURCE summary, VariantSwitcher correctly hidden (seed asset has no siblings), inspector unmounts to 0 height on deselect (inputs 3 → 0), Space-while-textarea-focused does NOT toggle play (keyboard bail-out works). **Known test-harness limitation**: `input.dispatchEvent(Event('input'))` + `.dispatchEvent(FocusEvent('blur'))` on React 19 controlled inputs updates the DOM value but does NOT trigger the React synthetic onBlur handler — so the inspector's numeric edit path was NOT verified end-to-end (no `clip-trimmed` event in the log after the test). The code path itself is correct and the inspector state updates as expected; this is a React testability gap, not a product bug. Real user blur in the browser works. 731/731 bun tests pass, tsc clean for ClipCraft files.
- **Plan 6 — Legacy viewer replication** (`2026-04-14-clipcraft-plan6-legacy-replication.md`) — faithful replication of the entire legacy ClipCraft viewer shell onto craft's domain model. Superseded the earlier "Plan 6 = TimelineOverview3D" / "Plan 7 = DiveCanvas" split in the upcoming list because the legacy UI is one cohesive shell and splitting it made no sense after gap analysis. Seven tasks landed in one overnight subagent-driven run: (1) Scene logical layer + persistence — `ProjectScene` type, `SceneContext`/`useScenes`, pure `resolveScene(scene, allClips)` helper so downstream code can iterate without calling hooks in loops; (2) `ClipCraftLayout` + `TimelineShell` shell with collapse/overview/exploded/dive modes and a floating ↑/↓ toggle; (3) `VideoPreview` + `CaptionOverlay` mounted on `<PreviewRoot>` with render-prop canvas ref, `useActiveSubtitle` walking subtitle tracks at `currentTime`; (4) `AssetPanel` Model B — craft asset registry IS the full material library (4 tabs: Images / Clips / Audio / Text), no filesystem scan, `useAssetActions` uploads via `POST /api/files?origin=external` + `asset:register` + `provenance:set-root`; (5) `TimelineOverview3D` — framer-motion 12 CSS 2.5D (perspective / rotateX/rotateY) with Front/Side presets, `VideoLayerContent` / `CaptionLayerContent` / `AudioLayerContent`, `FakeWaveform`; (6) `ExplodedView` — per-layer scaled stack, scroll-wheel to shift focus, `useCurrentFrame` subscribes to `subscribeToFrames` and redraws a hidden canvas; (7) `DiveCanvas` — `@xyflow/react@12` + `@dagrejs/dagre@1` DAG over `coreState.provenance.edges`, `VariantPointerContext` so `NodeShell`'s "Use This" updates the active variant for a clip. Fixed multiple plan-code bugs inline: hook-in-loop from `useSceneResolver` inside `useMemo` (replaced with pure `resolveScene`), `TreeNodeData` missing xyflow's `Record<string,unknown>` index signature, `AssetMetadata` strict field set (`originalName` moved to `operation.params`), `RenderedFrame.image` (not `.bitmap`), `useWorkspaceAssetUrl` signature widened to accept `null`. Pre-existing `ClipCraftPreview` provider nesting extended to `SyncedBody → TimelineZoomProvider → TimelineModeProvider → SceneProvider → VariantPointerProvider → AssetErrorsProvider → PneumaCraftProvider`. 698/698 tests pass at the end of Plan 6. **Browser-verified via `chrome-devtools-mcp`**: all four timeline modes (collapsed/overview/exploded/dive), VideoPreview playback 0→5s with painted canvas, AssetPanel 4 groups, Overview3D Front/Side presets + layer toggles (Video/Caption/Audio), ExplodedView side camera, DiveCanvas back/prev/next navigation, Script tab showing 2 seed scenes. Known deferred polish (per user "后面再一起修"): expanded mode hides VideoPreview play controls (want floating control bar), ExplodedView "Capturing frame" relies on active playback, video dive xyflow tree not exercised in test because time fell past clip range.
- **Plan 5.5 — Clip editing on timeline** (`2026-04-14-clipcraft-plan5.5-clip-editing.md`) — make the Plan 5 read-only timeline interactive. The ripple+snap drag algorithm was copied verbatim from `@pneuma-craft/react-ui/src/timeline/timeline-track.tsx` into a mode-local `timeline/dragEngine.ts` with unit tests; react-ui stays unconsumed. Reusable hooks: `useTrackDragEngine` (document-level mousemove/mouseup state machine with snap + ripple preview, dispatches a single `composition:move-clip` on release), `useClipResize` (left/right edge drag anchoring the opposite edge; dispatches `composition:trim-clip` always and `composition:move-clip` if the start shifted), `useTimelineShortcuts` (window keydown, bails out inside editable targets, wires Delete / S / ⌘Z / ⌘⇧Z). All three track components (`VideoTrack`, `AudioTrack`, `SubtitleTrack`) gained drag + 6px edge resize handles with a `grabbing` cursor toggle and an orange snap guide line. New `toolbar/EditToolbar.tsx` next to zoom controls renders Split / Delete / Collapse Gaps / Undo / Redo, gated on selection + `useUndo()`. `toolbar/collapseGaps.ts` walks each track in startTime order and emits sequential `move-clip`s to pack clips against 0. **Browser-verified via `chrome-devtools-mcp`** on a fresh `/tmp/clipcraft-plan5.5` workspace: (1) drag-to-move dispatched `composition:move-clip` and persisted to `project.json`, (2) click Split at playhead=2.4s produced two clips with `clip-1` inPoint 0→out 2.406 and a new clip inPoint 2.406→out 5 (confirming craft's splitter correctly carries inPoint/outPoint across the boundary), (3) Delete removed the selected half, (4) Collapse Gaps moved the orphan from start=2.406 back to start=0 with a single `move-clip`, (5) right-edge resize shrank duration 2.593→2.018s with `outPoint` → 4.424 and `inPoint` untouched, (6) left-edge resize shifted start 0→0.501 while `inPoint` 2.407→2.907 and right anchor (startTime+duration) held constant. 717/717 bun tests pass, `bun run tsc --noEmit` clean for ClipCraft files. **Known dev-only quirk**: under React StrictMode in Vite dev, a single drag occasionally dispatches two `move-clip` events (observed 0→2.0s instead of 0→1.0s); this is `useEffect` double-mount behavior and does not reproduce under production build. **Known pre-existing craft gotcha** (not plan 5.5): the undo manager wraps hydration events, so clicking Undo enough times undoes the initial composition-created / track-added / clip-added events and empties the project — Plan 5.5 did not fix this because it's a craft-store-level design issue for the upstream team. Multi-select is intentionally deferred. Toast-on-edit was dropped from scope (event log + composition state already reflect every edit).
- **Plan 5 — Timeline UI (read-only, legacy-faithful port)** (`2026-04-13-clipcraft-plan5-timeline.md`) — port the visual timeline from `clipcraft-legacy/viewer/timeline/` onto the craft store. The original Plan 5 implementation wrote 4 of the components from scratch with inline styles that didn't match legacy's compact layout; after visual inspection it was **reworked** to be a faithful port. New `modes/clipcraft/viewer/timeline/` contains: `Timeline.tsx` (legacy shell — compact 10px-font zoom header, "scroll / ⌘+scroll to zoom" hint, 2px GAP between rows, 32px `LABEL_W` with per-type emoji 🎬 / 🔊 / Tt), `VideoTrack.tsx` (absolute-positioned clips with 4px padding, filmstrip thumbnails via `useFrameExtractor`, subtle `rgba(249,115,22,0.3)` selection border — **no** chunky orange fill), `AudioTrack.tsx` (waveform bars), `SubtitleTrack.tsx` (ported from legacy `CaptionTrack`), `WaveformBars.tsx`, `TimeRuler.tsx` (ticks clamped to `[0, duration]`), `Playhead.tsx`, `TrackLabel.tsx` (verbatim 32px label), `hooks/useTimelineZoom.ts` (functional-updater setters so rapid `zoomIn` clicks accumulate correctly), `hooks/useFrameExtractor.ts`, `hooks/useWaveform.ts`. Click on ruler → `usePlayback().seek`; click on clip → `useDispatch(("human", { type: "selection:set", selection: { type: "clip", ids: [clipId] }}))`. Zoom is UI-only local state, not persisted. Ctrl/meta+wheel zoom uses a native passive-false listener so browser page zoom stays blocked. **Pre-task fix**: EditorPanel saves now pass `?origin=external` so writes through Pneuma's built-in editor reach the viewer's `Source<T>` as external events instead of being dropped as self-echoes. **Smoke-tested via `chrome-devtools-mcp`** after rework: filmstrip renders Cliper.mp4 thumbnails across 15s, click-to-seek lands within a pixel of the click position (60% → 8.65s on a 15s timeline), click-on-clip applies the subtle legacy selection border, 3× rapid zoom-in correctly accumulates (124 → 272 px/s = 124 × 1.3³), ruler no longer emits negative tick labels.

## Superseded

- **Plan 3d — Bidirectional sync transport (originally scoped, now absorbed upstream)** — Plan 3d would have hardened the mode-local `useProjectSync` hook with a proper transport contract. Instead, the upstream `feat/source-abstraction` work landed on `main` and introduced a first-class `Source<T>` abstraction with a `json-file` provider and server-side `pendingSelfWrites` origin tagging. ClipCraft was migrated to consume `sources.project` via `useSource` (commits `5319436`, `7582009`, `42bca41`, `06204f6`), deleting the entire mode-local sync layer (the bidirectional sync hook, the `externalEdit` helper, the `api-client`, and their tests). The "three-ref dance" is gone, and Plan 3b's load-bearing coupling between parent ref reset and provider remount no longer exists. See `docs/superpowers/plans/clipcraft-source-migration.md` and `docs/migration/2.29-source-abstraction.md`.

## Known limitations (to address in later plans)

- **"Full re-dispatch on external edit"** still kills in-memory state (undo history, PlaybackEngine playhead position) on every agent-originated file change. The source migration tags origin precisely (so the viewer knows *that* a change is external), but the response is still "bump providerKey, remount the craft store, re-hydrate from scratch". This is a craft-store-level concern — `Source<T>` cannot solve it. Plan 4 made this pain real (a live playing clip drops back to 0s and re-loads on every agent edit); a follow-up plan will replace the remount strategy with diff-and-dispatch.
- **Plan 5 leaves clip-level editing unwired.** The timeline is read-only for composition shape — click-to-select works, but drag-to-move, resize, and split do not. Plan 5.5 will port the ripple+snap drag engine from `@pneuma-craft/react-ui/src/timeline/timeline-track.tsx` by **copying the algorithm** into ClipCraft. react-ui stays unconsumed as a direct dep.
- **Still no automated test coverage for the rendering / timeline path itself.** The preview + timeline components are thin wrappers over craft hooks and pure data hooks from legacy. Verification is tsc + import-smoke tests + browser smoke tests via `chrome-devtools-mcp`. Once clip-edit lands and the drag math becomes non-trivial, install happy-dom + testing-library and cover the ripple/snap algorithm.

- **Plan 9 — Skill scripts + commands** (`2026-04-14-clipcraft-plan9-skill-scripts.md`) — subsumes what was originally split as Plan 9 (MCP tool integration) + Plan 10 (skill rewrite). After a design round-trip that briefly considered an MCP server port, a mode-level Hono `api` abstraction, and a unified `ModeApi` layer, the landing decision was **ship the simplest surface possible**: port the legacy fal.ai / OpenRouter generators into plain argv CLI scripts under `modes/clipcraft/skill/scripts/`, rewrite SKILL.md following [anthropics/skills/skill-creator](https://github.com/anthropics/skills/tree/main/skills/skill-creator) best practices (frontmatter + 125-line body + `references/` bundled docs), and add a `viewerApi.commands[]` toolbar of user-clickable buttons that fire `onNotifyAgent` with natural-language messages into the agent chat (like webcraft's design commands). Zero runtime changes — uses existing viewer-command notification path. Four generators landed: `generate-image.mjs` (fal.ai nano-banana-2, text→image + `edit` subcommand for image→image), `generate-video.mjs` (fal.ai veo3.1, text→video + `from-image` subcommand; **`--duration` is REQUIRED** to avoid a legacy foot-gun where a missing arg silently triggered a $1.60-4.80 8s generation), `generate-tts.mjs` (OpenRouter openai/gpt-audio SSE streaming, WAV output), `generate-bgm.mjs` (OpenRouter google/lyria-3-pro-preview SSE streaming, MP3 output, 180s timeout). Env vars split from legacy's ambiguous `API_KEY` into `FAL_KEY` + `OPENROUTER_API_KEY`. Three reference docs land under `modes/clipcraft/skill/references/`: `project-json.md` (schema), `workflows.md` (three end-to-end flows), `asset-ids.md` (id stability). SKILL.md pushes hard on its triggering description to combat undertriggering. Six viewer commands declared: Generate image, Generate video, Try another take, Add narration, Add BGM, Export video — each dispatches a `{ type: "clipcraft-command", message, severity: "warning" }` notification so the agent receives it. **Gap caught during verification**: the original plan declared commands in manifest but didn't render them in the viewer — the React component only destructured `sources` from `ViewerPreviewProps`. Patched inline with a new `CommandBar.tsx` component threaded through `ClipCraftPreview` → rendered above the main layout, editorial-dark theme tokens applied, icons picked from the existing library. **Browser-verified via `chrome-devtools-mcp` screenshot**: fresh session on `/tmp/clipcraft-plan9-verify`, skill-installer copies `SKILL.md` + `scripts/` + `references/` to `.claude/skills/pneuma-clipcraft/` correctly, CLAUDE.md's pneuma:start section points at the installed skill, dev server boots clean, CommandBar renders the six buttons above the asset panel + video preview with the correct icons and labels. Schema drift found between old SKILL.md and `persistence.ts` and fixed in `project-json.md`: (a) clip-level `volume` / `fadeIn` / `fadeOut` were undocumented; (b) uniqueness rules were vague — clip ids are unique composition-wide (per `CommandValidationError`), asset ids are globally unique in the registry; (c) subtitle clips in the seed share one stub asset and carry `text` on the clip rather than per-asset; (d) `composition.settings.sampleRate?` was missing. Legacy bugs surfaced: (1) `clipcraft-imagegen.mjs`'s duration fallback `|| "8s"` was the exact hazard the plan asked to close; (2) all four legacy MCPs shared `API_KEY`, making it impossible to run fal.ai + OpenRouter scripts from the same shell without re-exporting; both fixed in the ports. `bunx tsc --noEmit` clean for clipcraft/viewer. Defers: runtime abstractions (Mode API / backend routes), viewer-initiated direct generation without chat bounce, async status streaming, eval harness, and export (Plan 8). 11 commits landed: 4 script ports + 3 reference docs + SKILL.md rewrite + manifest commands + CommandBar wire-up + plan doc archive.

- **Plan 8 — Browser-side export** (`feat/clipcraft-by-pneuma-craft d24d3f7`) — wire the `export-video` viewer command directly to `@pneuma-craft/video`'s `createExportEngine()`. Zero backend — the ExportEngine composites + muxes an MP4 in the browser tab via MediaBunny, reusing the already-mounted `WorkspaceAssetResolver`, and the finished Blob auto-downloads. New `viewer/export/useExportVideo.ts` hook owns the engine lifecycle, progress state (0..1 via `engine.onProgress`), blob URL cleanup on unmount, and default options (mp4 / avc / aac / 8 Mbps / 192 kbps — engine infers fps+resolution from composition settings). New `viewer/export/ExportProgress.tsx` strip renders under the CommandBar when status ≠ idle: token-colored progress bar (accent while running, success/danger on terminal state), status label in tabular-nums during active export, auto-synthetic-click of a download anchor when status flips to `"done"`, "Download again" link + dismiss button after. `CommandBar` grew a `handlers?: Record<string, () => void>` prop so command ids in the map fire local handlers instead of chat notifications — only `export-video` uses this; the 5 AIGC commands still route through `onNotifyAgent` as debug shortcuts until the proper dive-in variant generation + asset panel creation flows land in a later plan. `ClipCraftPreview` restructured: the outer flex column + CommandBar + ExportProgress now live inside `SyncedBody` (which is inside `PneumaCraftProvider`) so the export hook can pull `composition` from the craft store and the already-mounted `assetResolver` from the parent closure. Manifest's `export-video` description rewritten — no more "Plan 8 will bundle" disclaimer. `bunx tsc --noEmit` clean for `modes/clipcraft/viewer/`. Fallback strategy for long videos / large assets: **the browser's MediaBunny muxer handles it in-tab** — the user's device is the constraint, not the pneuma backend. If a user hits device limits we'll file upstream (craft may need streaming / chunked write). One commit: `d24d3f7 feat(clipcraft): Plan 8 — browser-side export via @pneuma-craft/video`.

- **Plan 11 — AIGC creation dialogs (dive-in variant + asset panel create)** (`feat/clipcraft-by-pneuma-craft 2497941`) — replace the "click command button → agent guesses" pattern with a first-class in-app generation form, wired into the two places the user actually wants to kick off AIGC from: the dive panel (variants of an existing asset) and the asset panel (fresh creations). The viewer captures the full intent in a rich form; the agent gets a single, unambiguous structured notification it can execute without ambiguity. Landing pieces: **`viewer/generation/dispatchGeneration.ts`** owns the notification format (`[clipcraft:create-asset]` or `[clipcraft:generate-variant]` tag + human-readable summary + fenced JSON payload containing `prompt`, `kind`, `sub_kind`, `params`, `script`, `script_args`, `provenance_hint` + an 8-step agent handler instruction block). Variant mode sets `operation_type: "derive"` + `from_asset_id: <source.id>`; create mode sets `operation_type: "generate"` + `from_asset_id: null` — agent reads these verbatim instead of branching. **`viewer/generation/GenerationDialog.tsx`** is a centered editorial-dark modal with a type picker (image/video/audio + a sub-picker for audio between TTS and BGM), prompt textarea (seeded from source provenance in variant mode), and type-specific field groups: image (aspect ratio + width/height + optional style); video (`--duration` REQUIRED, 4s/6s/8s + 16:9/9:16 + 720p/1080p); tts (voice picker); bgm (loose duration hint). Type picker is locked when mode = variant so the source's kind propagates. Submit builds a `GenerationRequest` and dispatches one `onNotifyAgent` call. **`viewer/generation/useGenerationDialog.tsx`** is a React context that hoists the dialog once at the provider level and exposes `{ openForCreate(kind?), openForVariant(source, kind), close() }` — any descendant opens the same dialog without prop drilling. **`NodeShell`** (dive panel, per-node card in the provenance tree) grew a "✦ Variant" button next to "Use This" / "Active" — only rendered for image/video/audio nodes, not text. Clicking calls `openForVariant(source, kind)` with the node's current prompt pre-seeded, so the user tweaks rather than re-writes. **`AssetPanel`** grew a "✦ Create with AI" accent pill directly under the ASSETS/SCRIPT tabs — full-width, always visible. Clicking calls `openForCreate("image")` with the type picker unlocked so the user can switch. **`ClipCraftPreview`** wraps `SyncedBody` in `GenerationDialogProvider` inside the existing provider stack so both NodeShell (deep in the dive canvas tree) and AssetPanel (in the layout shell) resolve the same dialog context. **Skill docs** grew `Workflow 4 — Structured generation notifications from the viewer` in `references/workflows.md` — documents the tag format, shows the exact JSON shape for both create and variant, and spells out the 8-step handler pattern (parse → pick semantic id → pick output path → run script → register asset → add provenance edge per hint → **do NOT touch the timeline** (user will place the clip separately) → save). Handler is identical for both tags because `operation_type` + `from_asset_id` are pre-filled. **Debug shortcuts preserved:** the 5 CommandBar chat-notify AIGC buttons (Generate image / Generate video / Try another take / Add narration / Add BGM) are intentionally left in place as per the user's request — they're useful while wiring, and removing them costs nothing now. Export still runs directly in-browser (Plan 8 path unchanged). **`bunx tsc --noEmit` clean** for `modes/clipcraft/viewer/`. **Browser-verified via `chrome-devtools-mcp` screenshot**: the "✦ CREATE WITH AI" button renders as expected below the ASSETS/SCRIPT tabs, right above the Images group. Dialog open + variant button click flows are user-verifiable but can't be clicked from a read-tier screenshot session. One commit: `2497941 feat(clipcraft): AIGC creation — dive-in variant + asset panel dialogs`.

## Upcoming

All scheduled ClipCraft plans are complete. Next candidates (not yet scheduled):
- Runtime `ModeApi` abstraction — formalize the "mode-local HTTP routes + agent tool declarations + viewer client" pattern discussed during the Plan 9 design round-trip. Deferred until a second mode actually needs it.
- Drop the CommandBar AIGC debug shortcuts once the dialog flow is proven in real use — or repurpose the slot as selection-aware contextual commands (e.g. "split here" with playhead context).
- Inline generation progress in the dive panel + asset panel (the agent takes 30-120s for veo3.1; users need a local "generating…" indicator on the source node while the call is in flight).

## Rules of engagement

Before each plan:

1. Survey what `clipcraft-legacy` does for the relevant surface.
2. Decide if a new concept belongs in the mode or upstream in `@pneuma-craft/*`. Discuss with the user.
3. Write the plan, verifying all referenced types against real `.d.ts` files (not the survey).
4. Use subagent-driven execution with combined spec + quality reviews between tasks.

`@pneuma-craft/*` lives at `/Users/pandazki/Codes/pneuma-craft-headless-stable` and is under active development (`0.1.0`, "not yet stable"). Patches to the packages land there on branch `feat/clipcraft-aigc-status`, not in this repo.
