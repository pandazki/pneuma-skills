// modes/<name>/manifest.ts
//
// Template generated by .claude/skills/create-mode.
// Replace every TODO with values from the design brief; delete optional
// sections that don't apply.

import type { ModeManifest } from "../../core/types/mode-manifest.js";

const manifest: ModeManifest = {
  // ── Identity ────────────────────────────────────────────────────────────
  name: "TODO_NAME",  // kebab-case, same as directory name
  version: "0.1.0",
  displayName: {
    en: "TODO Display Name",
    "zh-CN": "TODO 显示名",
  },
  description: {
    en: "TODO one-line description for launcher cards.",
    "zh-CN": "TODO 给 launcher 卡片用的一句话描述。",
  },
  // SVG inline. Keep it simple — one or two paths, no fills besides currentColor.
  icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><!-- TODO --></svg>`,

  // ── Skill ───────────────────────────────────────────────────────────────
  skill: {
    sourceDir: "skill",
    installName: "pneuma-TODO_NAME",
    mdScene: "TODO: A short scene-setting paragraph (1-3 sentences) describing what the user and agent are doing together in this mode. See references/skill-md-patterns.md::Scene.",
    // envMapping: { ENV_VAR_NAME: "initParamName" },   // Uncomment if any init.params has sensitive: true
    // mcpServers: [{ ... }],                            // Uncomment if mode needs MCP tooling
    // sharedScripts: ["generate_image.mjs"],            // Uncomment if mode reuses modes/_shared/scripts/
  },

  // ── Viewer (file watching) ─────────────────────────────────────────────
  viewer: {
    watchPatterns: ["**/*.html"],  // TODO: adjust to your mode's content files
    ignorePatterns: ["node_modules/**", "dist/**", ".pneuma/**"],
    // refreshStrategy: "manual",  // Uncomment if viewer needs debounced re-render (e.g. Babel JIT)
  },

  // ── Agent preferences ───────────────────────────────────────────────────
  agent: {
    permissionMode: "bypassPermissions",
    // greeting: "TODO: optional template for the agent's first response in a new session",
  },

  // ── Init (params + seed) ────────────────────────────────────────────────
  init: {
    contentCheckPattern: "**/*.html",  // TODO: pattern for "workspace is non-empty"
    seedFiles: {
      // TODO: { workspaceRelativePath: modePackageRelativePath }
      // Example for a single content set:
      //   "pneuma-default/index.html": "seed/pneuma-default/index.html",
      //   "pneuma-default/manifest.json": "seed/pneuma-default/manifest.json",
    },
    params: [
      // TODO: list any init parameters. Each becomes a value the user provides
      // on first launch and can be templated into seed files as {{name}}.
      // Example:
      //   {
      //     name: "primaryFont",
      //     label: "Primary font family",
      //     type: "select",
      //     options: ["Inter", "DM Serif Display"],
      //     defaultValue: "Inter",
      //   },
      // For API keys:
      //   {
      //     name: "openaiApiKey",
      //     label: "OpenAI API Key",
      //     description: "Stored locally, never sent to Pneuma.",
      //     type: "string",
      //     defaultValue: "",
      //     sensitive: true,
      //   },
    ],
    // deriveParams(params) {
    //   return { /* derived flags / values from raw params */ };
    // },
  },

  // ── Viewer API (what agent / framework see) ─────────────────────────────
  viewerApi: {
    workspace: {
      type: "manifest",   // TODO: "all" | "manifest" | "single"  — see references/viewer-contract-patterns.md
      multiFile: true,
      ordered: true,
      hasActiveFile: true,
      manifestFile: "manifest.json",  // Only if type === "manifest"
      supportsContentSets: true,      // Only if mode supports multiple content sets
    },
    actions: [
      // TODO: 2-5 actions. Each is a verb the agent can invoke on the viewer.
      // Don't list `capture` — it's framework-built-in.
      // Example:
      {
        id: "navigate-to",
        label: "Navigate to TODO",
        category: "navigate",
        agentInvocable: true,
        description:
          "Move the viewer to a specific TODO_NOUN by address. Use after editing so the user can review.",
        params: {
          address: {
            type: "object",
            description: "ViewerAddress for the target object, e.g. `{ TODO_KEY: ... }`",
            required: true,
          },
        },
      },
    ],
    commands: [
      // TODO: 0-N commands. Each surfaces a button in the viewer that, on click,
      // notifies the agent. Use for "user-triggered tasks" (vs actions which
      // are agent-triggered).
      // Example:
      //   {
      //     id: "regenerate",
      //     label: "Regenerate",
      //     description: "User asks the agent to redo the current TODO_NOUN with fresh ideas.",
      //   },
    ],
    // scaffold: { ... },  // Uncomment if the mode supports initializing into empty workspace
  },

  // ── Sources (typed data channels) ───────────────────────────────────────
  sources: {
    // TODO: declare one or more typed data channels for the viewer to consume.
    // See references/domain-and-sources.md for kind selection.
    //
    // Common shapes:
    //
    // (a) aggregate-file — multi-file domain (most common for content modes):
    //   doc: {
    //     kind: "aggregate-file",
    //     config: {
    //       patterns: ["**/*.html", "**/manifest.json"],
    //       ignore: ["node_modules/**"],
    //       load: (files) => loadDomain(files),    // pure fn from domain.ts
    //       save: (next, current) => saveDomain(next, current),
    //     },
    //   },
    //
    // (b) file-glob — many independent files, no aggregation:
    //   files: {
    //     kind: "file-glob",
    //     config: { patterns: ["**/*.tsx"], ignore: ["node_modules/**"] },
    //   },
    //
    // (c) json-file — single structured doc:
    //   project: {
    //     kind: "json-file",
    //     config: {
    //       path: "project.json",
    //       parse: (raw) => JSON.parse(raw) as Project,
    //       serialize: (p) => JSON.stringify(p, null, 2),
    //     },
    //   },
    //
    // (d) memory — ephemeral:
    //   ui: { kind: "memory", config: { initial: { /* ... */ } } },
  },

  // ── Evolution (give evolve agent a direction) ──────────────────────────
  evolution: {
    directive:
      "TODO: a one-sentence directive for the evolve agent — what should it learn for this mode and how does that learning get used? See references/skill-md-patterns.md::Evolution.",
  },

  // ── Optional metadata ──────────────────────────────────────────────────
  // Uncomment any that apply; delete the rest.

  // changelog: {
  //   "0.1.0": ["Initial release."],
  // },

  // supportedBackends: ["claude-code"],   // Restrict backends; default is all implemented

  // inspiredBy: {                          // Non-binding attribution (for launcher gallery)
  //   name: "TODO upstream name",
  //   url: "TODO https://...",
  // },

  // layout: "app",                         // "editor" (default) or "app" (fullscreen viewer + floating chat)
  // window: { width: 1400, height: 900 },  // Electron startup window size

  // editing: { supported: true },          // Enable view/edit toggle; default is always editing

  // proxy: {
  //   TODO_NAME: {
  //     target: "https://upstream.example.com",
  //     methods: ["GET"],
  //     description: "TODO: what this proxies and why",
  //   },
  // },

  // pneumaVersion: "^3.12.0",              // Only for external modes; built-in modes ship with the runtime

  // hidden: true,                          // Only for internal modes (evolve, project-onboard)
};

export default manifest;
