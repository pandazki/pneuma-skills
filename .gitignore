node_modules/
dist/
dist-player/
.DS_Store
*.log
.env
.env.*
!.env.example
.pneuma/

# Landing page deploy config
web/.deploy.env

# Superpowers
.superpowers/

# Antigravity CLI local state
.antigravitycli/

# Git worktrees
.worktrees/
.claude/worktrees/

# Claude Code local state; shared repository skill adapters are tracked below
.claude/skills/*
!.claude/skills/create-mode
!.claude/skills/dev-workflow
.claude/e2e-*.png
.claude/*.lock
.claude/settings.local.json

# Per-mode build artifacts (e.g. modes/slide/.build/)
.build/
# Mode distribution: catalog.json is generated per release by
# scripts/publish-modes.ts (and fetched by CI) — the package ships it through
# package.json "files", it is never hand-edited. .publish/ stages the archives.
modes/catalog.json
.publish/
# Blind-trial screenshots a browser-driving agent can only write under the repo root
.tmp-*/
e2e-*-shots/
